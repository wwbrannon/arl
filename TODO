o) classes that have instances of other classes horizontally as attributes

o) More tests for .rye code: download the hy test suite locally and compare to it

o) How to calculate test coverage of .rye code? this is probably too complicated

o) performance optimizations: use profiling and benchmarking

o) minor pkgdown warning about site url
