o) examples from docs to examples format proper and more of them

big features:
    o) what else needs to be in the stdlib? other common lisp features
    o) more tests; big integration-type tests? coverage check?
    o) sanity-check the tests: they all pass, but does the tested behavior match how lisp ought to behave?
    o) better docs?

inst/CITATION?


