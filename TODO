o) rewrite cli to use docopt and generally simplify cli
o) move cli_load_env into a classmethod ctor of RyeEngine / retire load_stdlib

o) move out the rest of stdlib.R

o) OO refactor of repl

o) classes that have instances of other classes horizontally as attributes
o) do we still need to assign these macro registry etc into the environment?
o) unlockBinding note

o) performance optimizations: use profiling and benchmarking
o) Look for any R code that’s left over or not used
o) Look for any R code that can be refactored to make it simpler
o) How to calculate test coverage of .rye code
o) More tests for .rye code
o) pkgdown warning about site url
