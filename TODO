o) make clean && make document && make profile && make bench && make coverage && make check
o) git push and wait for tests to complete
o) figure out citation
o) devtools::release

o) figure out how to host the docs
o) add url, links
